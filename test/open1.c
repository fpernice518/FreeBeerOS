#include "syscall.h"

int main()
{
  // OpenFileId fd;
  Write("creat1\n\r", 8, ConsoleOutput);
  Write("create2\n\r", 8, ConsoleOutput);
  Write("create3\n\r", 8, ConsoleOutput);
  // fd = Open("create-test");
  // Close(fd);
}
